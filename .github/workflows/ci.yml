name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  test:
    runs-on: ubuntu-latest-24.04
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12", "3.13"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e ".[dev,all,web]"

      - name: Debug environment
        run: |
          python --version
          python -m pytest --version
          python -c "import nexus_agent_ai; print('src OK')"
          python -c "from nexus_agent_ai.agent.tools import _sandbox_check; print('tools OK:', _sandbox_check('x=1'))"
          python -c "from nexus_agent_ai.agent.core import Agent; print('core OK')"
          python -c "from nexus_agent_ai.cli import display; print('display OK')"

      - name: Run tests
        run: python -m pytest tests/ -v --tb=short
