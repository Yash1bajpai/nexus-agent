typer>=0.12.0
rich>=13.0.0
anthropic>=0.30.0; sys_platform != 'android' and platform_system != 'Android' and not (platform_system == 'Linux' and platform_machine == 'aarch64')
openai>=1.35.0; sys_platform != 'android' and platform_system != 'Android' and not (platform_system == 'Linux' and platform_machine == 'aarch64')
google-genai>=0.1.0
duckduckgo-search>=6.0.0
python-dotenv>=1.0.0
psutil>=5.9.0; sys_platform == 'win32' or sys_platform == 'darwin'
pytest>=8.0.0
huggingface-hub>=0.24.0